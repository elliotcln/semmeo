<nav class="skiplinks" aria-label="liens d'accès rapide">
  <a href="#main-navbar">Accéder directement au menu</a>
  <a href="#main-content">Accéder directement au contenu</a>
</nav>

<style lang="scss">
  .skiplinks {
    text-align: center;
    position: absolute;
    inset-inline: 0;
    top: 0;
    z-index: 999;
    transform: translateY(-100%);
    will-change: transform;
    transition: transform 0.3s ease;

    &:focus-within {
      transform: translateY(0);
    }

    a {
      display: block;
      padding: 0.5rem;
      background-color: var(--c-dark);
      text-decoration: none;

      font-size: 0.825rem;
      color: var(--c-light);

      &:focus-visible,
      &:hover {
        background-color: var(--c-purple);
        color: var(--c-dark);
      }

      @media (width > 64rem) {
        display: inline-block;
      }
    }
  }
</style>
